<%= form_for :answer , url: answer_tutorial_path(@tutorial) do |f| %>
<!-- <div id="modal-content">
   <p>「閉じる」か「背景」をクリックするとモーダルウィンドウを終了します。</p>
   <p><a id="modal-close" class="button-link">閉じる</a></p>
 -->

    <div class="question-form">
        <div class="question-white">
        <fieldset class="fieldset">
            <div class="exercise-title">
                <h2>Exercise</h2>
                <p>復習問題を解きましょう</p>
            </div>

            <div class="questions">
                <% @tutorial.questions.each_with_index do |question, q_index| %>
                  <div class="question"> 
                    <div class="question-title">
                        <h3>Question<%= q_index + 1 %></h3>
                        <p><%= question.question %></p>
                    </div>
                    <div class="clear"></div>
                    <ul>
                        <% question.selects.each_with_index do |select, index| %>
                        <li>
                            <label><%= f.radio_button "content#{q_index}", index+1 %><%= select.content %></label>
                        </li>
                        <% end %>
                    </ul>
                  </div>
                <% end %>           
            </div>

           <!--  <div class="clear"></div> -->
         
            <input type="submit" value="提出", class="btn btn-pink">

        </fieldset>
        </div>
    </div>
<!-- </div> -->
<% end %>